# Screenshots

Drop the README screenshots here:

- `dashboard.png` — the org dashboard (usage panel + knowledge stats)
- `ask.png` — the Ask view with a cited answer

These paths are referenced from the main [README](../../README.md). PNG or JPG
both work; keep them a reasonable width (~1400px) so they render crisply without
bloating the repo.
